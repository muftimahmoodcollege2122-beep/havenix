# DATABASE_URL=postgresql://user:pass@host:5432/havenix
# PORT=4000

# Admin panel access key — required to log into /admin.
# Set a strong, unique value in Railway's environment variables.
ADMIN_KEY=havenix-admin
